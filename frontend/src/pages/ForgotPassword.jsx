import React, { useState } from 'react'; import { Link } from 'react-router-dom'; import '../styles/auth.css';
export default function ForgotPassword(){const [email,setEmail]=useState('');const [otp,setOtp]=useState('');const [password,setPassword]=useState('');const [sent,setSent]=useState(false);const [message,setMessage]=useState('');const send=async e=>{e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message);if(r.ok)setSent(true);};const reset=async e=>{e.preventDefault();const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,otp,password})});const d=await r.json();setMessage(d.message);if(r.ok)setSent(false);};return <div className="auth-container"><form className="auth-form" onSubmit={sent?reset:send}><h2>Reset password</h2><p className="verification-copy">{sent?'Enter the code sent to your email.':'We will email you a six-digit reset code.'}</p><input type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/>{sent&&<><input inputMode="numeric" maxLength="6" required placeholder="6-digit code" value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,''))}/><input type="password" required minLength="6" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)}/></>}{message&&<p className="auth-message">{message}</p>}<button className="btn">{sent?'Update password':'Send reset code'}</button><p><Link to="/login">Back to login</Link></p></form></div>}
